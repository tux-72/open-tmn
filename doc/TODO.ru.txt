Реализовать связку с текущим биллингом на стадии авторизации более универсальным
способом - через RADIUS-proxy, возможно имитируя запросы ISG.

Механизмы определения точек доступа и синхронизации данных с биллинговой системой
перенести в соответствующие функции rlm_perl связанные с проксированием запросов

Алгоритм учёта точек доступа и признака доверенной точки перенести полностью в
логику авторизации RADIUS-proxy.

Реализовать механизм взаимодействия с внешним шейпером в процессе авторизации
через pipe inode + ssh

